pre-commit:
  parallel: true
  commands:
    biome-check:
      glob: "*.{js,jsx,ts,tsx,json,css}"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    build:
      run: pnpm build
