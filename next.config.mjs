/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath:'/mirzaazwad',
    output:'export',
    distDir:'dist',
    images:{
        unoptimized:true
    }
};

export default nextConfig;
